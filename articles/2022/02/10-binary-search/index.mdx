---
title: "Binary Search"
date: 2022-02-13
slug: binary-search
description: ""
tags: ["algorithms"]
---

In order to perform Binary Search, the input should be **sorted**. Binary search reduces the task by half in each step.

If an element is present in the list, Binary Search returns the position where it is located. Otherwise, binary search returns `null`.

## Binary Search Implementation in JavaScript

We are going to write a function `binarySearch()` that accepts two arguments. One argument is a **sorted array** and other one is the **search term**.

```javascript
function binarySearch(sortedArray, searchTerm) {}
```

## Where do we apply Binary Search in life?

We are following the algorithm behind binary search while doing following things.

- Search for a phone number in phone book
- Search for a word meaning in dictionary
- Find a user from user table in data base
