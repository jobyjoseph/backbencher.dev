---
title: Quiz - JavaScript Arrays
date: 2021-02-08
slug: javascript-quiz-arrays
description: Here is a bunch of questions related to JavaScript arrays. It helps a developer to test his/her knowledge about JavaScript arrays.
tags: ["javascript"]
---

import Answer from "../../../../src/components/answer";
import QuestionItem from "../../../../src/components/questionitem";
import Question from "../../../../src/components/question";
import Option from "../../../../src/components/option";
import Explanation from "../../../../src/components/explanation";
import Divider from "../../../../src/components/divider";

Here is a bunch of questions related to JavaScript arrays. It helps a developer to test his/her knowledge about JavaScript arrays.

<QuestionItem>

<Question>

**Question 1:**

Which of the following is / are valid statement(s) to declare an array?

A. `const arr = [2, 4, 6];`

B. `const arr = new Array(2, 4, 6);`

C. `const arr = Array(2, 4, 6);`

</Question>

<Option>

A and B

</Option>
<Option>

Only A

</Option>
<Option>

B and C

</Option>
<Option isAnswer="yes">

A, B and C

</Option>

<Explanation>

**Explanation:** <br/>
Declaring an array using square brackets(`[]`) is common. Talking about `Array` function in JavaScript, it can be used both as a constructor and a normal function. That means, `Array()` is an overloaded function in JavaScript. We can use both the techniques to declare an array.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 2:**

What is the output?

```javascript
const arr = [2, , , 4];
console.log(arr.length);
```

</Question>

<Option>

`5`

</Option>
<Option isAnswer="yes">

`4`

</Option>
<Option>

`2`

</Option>
<Option>

`SyntaxError: Invalid operator ,`

</Option>

<Explanation>

**Explanation:** <br/>
In JavaScript arrays, we can skip elements by using comma operator. Here `[2,,,4,]` is equivalent to `[2, undefined, undefined, 4]`. Even though there is a comma at last, that is ignored. The last comma does not create an `undefined` element at the end of array.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 3:**

What is the output?

```javascript
const arr = [, , ,];
console.log(arr.length);
```

</Question>

<Option isAnswer="yes">

`3`

</Option>
<Option>

`2`

</Option>
<Option>

`4`

</Option>
<Option>

`1`

</Option>

<Explanation>

**Explanation:** <br/>
`[,,,]` is equivalent to `[undefined, undefined, undefined]`. So the length of the array is `3`.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 4:**

What is the output?

```javascript
const arr = Array(3);
console.log(arr);
```

</Question>

<Option>

`undefined`

</Option>
<Option>

`[3]`

</Option>
<Option isAnswer="yes">

`[undefined, undefined, undefined]`

</Option>
<Option>

None of these

</Option>

<Explanation>

**Explanation:** <br/>
`Array()` function in JavaScript can be used to declare arrays. When more than one argument is passed to `Array()` function it creates an array with all the passed arguments as array elements.

```javascript
const arr = Array(3, 6, 9);
console.log(arr); // [3, 6, 9]
```

If a **single numeric argument** is passed to `Array()`, it takes the argument as the length of the array.

```javascript
const arr = Array(3);
console.log(arr); // [undefined, undefined, undefined]
```

If a single non-numeric argument is passed to `Array()`, it is taken as the array element.

```javascript
const arr = Array("3");
console.log(arr); // ["3"]
```

> When we use `Array.of()`, even if we pass a single numeric argument, that number forms the array element. It is not taken as the length of array.

```javascript
console.log(Array(3)); //[undefined, undefined, undefined]
console.log(Array.of(3)); // [3]
```

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 5:**

What is the output?

```javascript
const arr = ["Mercedes", "BMW", "Audi"];
console.log(arr[1]);
```

</Question>

<Option isAnswer="yes">

`"BMW"`

</Option>
<Option>

`"Mercedes"`

</Option>
<Option>

`"Audi"`

</Option>
<Option>

`undefined`

</Option>

<Explanation>

**Explanation:** <br/>
Arrays are indexed starting from `0`. In that case, `arr[0]` contains `"Mercedes"`. `arr[1]` contains `"BMW"`.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 6:**

What is the output?

```javascript
const arr = ["Mercedes", "BMW", "Porsche"];
console.log(arr[1000]);
```

</Question>

<Option isAnswer="yes">

`undefined`

</Option>
<Option>

`ReferenceError: Array out of bound exception`

</Option>
<Option>

`null`

</Option>
<Option>

`""`

</Option>

<Explanation>

**Explanation:** <br/>
Arrays in JavaScript are objects. If we try to access an element or a key of object, which is not present, JavaScript returns `undefined`.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 7:**

What is the output?

```javascript
const arr = ["Apple", "Banana", "Orange"];
console.log(arr["2"]);
```

</Question>

<Option>

`undefined`

</Option>
<Option isAnswer="yes">

`"Orange"`

</Option>
<Option>

`TypeError: Invalid array index`

</Option>
<Option>

`"Banana"`

</Option>

<Explanation>

**Explanation:** <br/>
The elements in a JavaScript array is stored as key-value pairs internally. So an element at index 2 can be accessed using `arr[2]` or `arr["2"]`. Both are valid statements.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 8:**

In what all ways, we can check if a variable `arr` contains an array in JavaScript?

A. `Array.isArray(arr) === true`

B. `arr.constructor.name === "Array"`

C. `typeof arr === "Array"`

</Question>

<Option isAnswer="yes">

A and B

</Option>
<Option>

Only A

</Option>
<Option>

B and C

</Option>
<Option>

A, B and C

</Option>

<Explanation>

**Explanation:** <br/>
`isArray()` is a static method of `Array()` function which can be used to check if a value is an array or not.

Option _B_ also works. Every array is an instance of `Array()` function. So the `constructor.name` always point to the name of `Array()` function, ie `"Array"`.

Option _C_ does not work. It is because `typeof arr` returns `"Object"` instead of `"Array"`. So we cannot use `typeof` operator.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 9:**

What is the output?

```javascript
const arr1 = [1, 3, 5];
const arr2 = [2, 4, 6];
arr1.concat(arr2);
console.log(arr1);
```

</Question>

<Option>

`[1, 2, 3, 4, 5, 6]`

</Option>
<Option>

`[2, 4, 6]`

</Option>
<Option>

`[1, 3, 5, 2, 4, 6]`

</Option>
<Option isAnswer="yes">

`[1, 3, 5]`

</Option>

<Explanation>

**Explanation:** <br/>
`concat()` method merges `arr1` and `arr2` and returns the merged array. It does not affect `arr1` or `arr2`.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 10:**

```javascript
const arr1 = ["Apple", "Banana"];
const arr2 = arr1.concat();
arr2[0] = "Pineapple";
console.log(arr1);
```

</Question>

<Option isAnswer="yes">

`["Apple", "Banana"]`

</Option>
<Option>

`["Pineapple", "Banana"]`

</Option>
<Option>

`SyntaxError: missing argument for concat()`

</Option>
<Option>

`["Apple", "Banana", "Pineapple"]`

</Option>

<Explanation>

**Explanation:** <br/>
`Array.prototype.concat()` methods returns a **new array**. Also, it is not mandatory to pass an argument to `concat()` method. If we are not passing an argument, `concat()` method returns a shallow copy of `arr1` and assign to `arr2`. That is why, when the element in `arr2` is changed, `arr1` is not affected.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 11:**

What is the output?

```javascript
const arr = [2, 4, 6];
const result = arr.every((ele) => ele * 2);
console.log(result);
```

</Question>

<Option isAnswer="yes">

`true`

</Option>
<Option>

`[4, 8, 12]`

</Option>
<Option>

`false`

</Option>
<Option>

`[2, 4, 6]`

</Option>

<Explanation>

**Explanation:** <br/>
`every()` method of an array returns a boolean value. If the callback function returns a truthy value for all elements, `every()` returns `true`. In this case, the callback function returns non-zero number in each execution. Therefore the result is `true`. `every()` does not affect the original array.

</Explanation>
</QuestionItem>

<Divider />

<QuestionItem>

<Question>

**Question 12:**

What is the output?

```javascript
const arr = [1, 3, 5, 7];
console.log(arr.fill(6, 1, 3));
```

</Question>

<Option isAnswer="yes">

`[1, 6, 6, 7]`

</Option>
<Option>

`[1, 6, 6, 6]`

</Option>
<Option>

`[6, 1, 3, 7]`

</Option>
<Option>

`[6, 1, 3, 6]`

</Option>

<Explanation>

**Explanation:** <br/>

Array `fill()` method is used to fill the array with static values. It accepts 3 arguments. First argument `6` is the value to be filled. Second argument `1` represents the start position, from where the filling starts. The third argument `3` denotes the end position. The end index is not inclusive. Therefore, only elements at index `1` and `2` are replaced with `6`.

</Explanation>
</QuestionItem>
