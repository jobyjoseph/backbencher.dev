---
title: How to Render Span Instead of Div in Material UI Box Element
date: 2021-08-18
slug: how-to-render-span-instead-of-div-material-ui-box-element
description: Box element in Material UI by default renders div. We can pass a component property to render span.
tags: ["materialUI"]
---

Box element in Material UI by default renders `div`. We can pass a `component` property to render `span`.

```html
<Box component="span" m="{1}">
  <button />
</Box>
```
